---
apiVersion: v1
kind:  ConfigMap
metadata:
  name: {{ kubernetes_prometheus_dashboard_prefix }}-{{ item.name }}
  namespace: {{ item.namespace | default('monitoring') }}
  labels:
    grafana_dashboard: "1"
data:
  {{ item.filename }}: |
    {{ item.json | indent(width=4, first=False) }}
